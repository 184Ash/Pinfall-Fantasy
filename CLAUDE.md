## Python
Always use `uv run python` instead of `python` or `python3` directly.
Always use `uv run pytest` instead of `pytest`, etc.

## Browser
Always use Microsoft Edge (not Chrome) for all browser automation tasks.
